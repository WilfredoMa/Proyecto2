import { MoviesOffPipe } from './movies-off.pipe';

describe('MoviesOffPipe', () => {
  it('create an instance', () => {
    const pipe = new MoviesOffPipe();
    expect(pipe).toBeTruthy();
  });
});
